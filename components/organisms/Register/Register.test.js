describe("Register page", () => {
  it("should show error", () => {
    const sum = 1 + 2;
    expect(sum).toBe(4);
  });
  it("should render", () => {
    const sum = 1 + 2;
    expect(sum).toBe(3);
  });
});
